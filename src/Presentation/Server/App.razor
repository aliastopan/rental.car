@using UserPage = Common.Component.Pages.Features.Account.UserPageBase
@using AuthPage = Common.Component.Pages.Features.Account.AuthPageBase
@using Dashboard = Common.Component.Pages.Features.Rental.Dashboard

@* @using CarGallery = Common.Component.Pages.Features.Rental.CarGallery *@

<CascadingAuthenticationState>
    <Router AppAssembly="@typeof(App).Assembly"
        AdditionalAssemblies="new[] {
            typeof(UserPage).Assembly,
            typeof(AuthPage).Assembly,
            typeof(Dashboard).Assembly
        }">
        <Found Context="routeData">
            <RouteView RouteData="@routeData" DefaultLayout="@typeof(MainLayout)" />
            <FocusOnNavigate RouteData="@routeData" Selector="h1" />
        </Found>
        <NotFound>
            <PageTitle>Not found</PageTitle>
            <LayoutView Layout="@typeof(MainLayout)">
                <p role="alert">Sorry, there's nothing at this address.</p>
            </LayoutView>
        </NotFound>
    </Router>
</CascadingAuthenticationState>